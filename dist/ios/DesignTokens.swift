// Desnify Foundations — generated by Style Dictionary. DO NOT EDIT.
import UIKit

// Hex string → UIColor. Supports #RGB, #RRGGBB, #RRGGBBAA.
private extension UIColor {
    convenience init(hex string: String) {
        var hex = string.trimmingCharacters(in: CharacterSet.alphanumerics.inverted)
        var int: UInt64 = 0
        Scanner(string: hex).scanHexInt64(&int)
        let a, r, g, b: UInt64
        switch hex.count {
        case 6:  (a, r, g, b) = (255, int >> 16, int >> 8 & 0xFF, int & 0xFF)
        case 8:  (a, r, g, b) = (int >> 24, int >> 16 & 0xFF, int >> 8 & 0xFF, int & 0xFF)
        default: (a, r, g, b) = (255, 0, 0, 0)
        }
        self.init(
            red: CGFloat(r) / 255,
            green: CGFloat(g) / 255,
            blue: CGFloat(b) / 255,
            alpha: CGFloat(a) / 255
        )
    }
}

struct DS {
    static let colorFgBrandPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#7271e3")
            : UIColor(hex: "#655bd7")
    }
    static let colorFgBrandSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#3a3679")
            : UIColor(hex: "#e3e8fc")
    }
    static let colorFgErrorPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#e94a55")
            : UIColor(hex: "#d5293d")
    }
    static let colorFgErrorSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#811a2f")
            : UIColor(hex: "#fde7e6")
    }
    static let colorFgWarningPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#f98824")
            : UIColor(hex: "#ec7004")
    }
    static let colorFgWarningSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#80390e")
            : UIColor(hex: "#fff0d5")
    }
    static let colorFgSuccessPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#1cad79")
            : UIColor(hex: "#109367")
    }
    static let colorFgSuccessSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#0b4937")
            : UIColor(hex: "#d4f7e4")
    }
    static let colorFgWhite = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#ffffff")
    }
    static let colorFgQuinary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#0a0d14")
    }
    static let colorFgQuaternary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#e0e6ef")
            : UIColor(hex: "#454f5e")
    }
    static let colorFgTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#c4cedc")
            : UIColor(hex: "#647287")
    }
    static let colorFgSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#303842")
            : UIColor(hex: "#e0e6ef")
    }
    static let colorFgPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#22282f")
            : UIColor(hex: "#f1f5f9")
    }
    static let colorFgBrandPrimaryAlt = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#e3e8fc")
            : UIColor(hex: "#655bd7")
    }
    static let colorFgSolid = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#161922")
            : UIColor(hex: "#0a0d14")
    }
    static let colorContentWhite = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#ffffff")
    }
    static let colorContentBrandSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#433b98")
    }
    static let colorContentBrandPrimaryAlt = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#655bd7")
    }
    static let colorContentPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#0a0d14")
    }
    static let colorContentSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffffcc")
            : UIColor(hex: "#454f5e")
    }
    static let colorContentTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff99")
            : UIColor(hex: "#647287")
    }
    static let colorContentInversePrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#0a0d14")
            : UIColor(hex: "#ffffff")
    }
    static let colorContentInverseSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#454f5e")
            : UIColor(hex: "#ffffffcc")
    }
    static let colorContentInverseTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#647287")
            : UIColor(hex: "#ffffff99")
    }
    static let colorContentErrorSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#961b30")
    }
    static let colorContentErrorPrimaryAlt = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#f8fafc")
            : UIColor(hex: "#d5293d")
    }
    static let colorContentWarningSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#9f440d")
    }
    static let colorContentWarningPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#f98824")
            : UIColor(hex: "#ec7004")
    }
    static let colorContentSuccessSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#0c5941")
    }
    static let colorContentSuccessPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#1cad79")
            : UIColor(hex: "#109367")
    }
    static let colorContentInverseBrand = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#655bd7")
            : UIColor(hex: "#7271e3")
    }
    static let colorContentPlaceholder = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#647287")
            : UIColor(hex: "#92a0b5")
    }
    static let colorContentBlack = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#0a0d14")
            : UIColor(hex: "#0a0d14")
    }
    static let colorContentBrandPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#7271e3")
            : UIColor(hex: "#655bd7")
    }
    static let colorContentErrorPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#e94a55")
            : UIColor(hex: "#d5293d")
    }
    static let colorBgSurfaceBrand = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#232046")
            : UIColor(hex: "#f0f3fd")
    }
    static let colorBgSurfacePrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#0a0d14")
            : UIColor(hex: "#ffffff")
    }
    static let colorBgSurfaceSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#161922")
            : UIColor(hex: "#f8fafc")
    }
    static let colorBgSurfaceTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#22282f")
            : UIColor(hex: "#f1f5f9")
    }
    static let colorBgSurfaceQuaternary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#303842")
            : UIColor(hex: "#e0e6ef")
    }
    static let colorBgSurfaceInverse = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#0a0d14")
    }
    static let colorBgSurfaceError = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#480915")
            : UIColor(hex: "#fef2f2")
    }
    static let colorBgSurfaceWarning = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#451b05")
            : UIColor(hex: "#fff8ed")
    }
    static let colorBgSurfaceSuccess = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#05291f")
            : UIColor(hex: "#edfcf5")
    }
    static let colorBg = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#0a0d14")
            : UIColor(hex: "#ffffff")
    }
    static let colorBgAlt = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#161922")
            : UIColor(hex: "#f1f5f9")
    }
    static let colorBgSurfaceBrandAlt = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#161922")
            : UIColor(hex: "#f0f3fd")
    }
    static let colorBgSurfaceTransparent = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#0a0d1400")
            : UIColor(hex: "#ffffff00")
    }
    static let colorBgSurfaceOverlay = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#0a0d1499")
            : UIColor(hex: "#0a0d1466")
    }
    static let colorBgSurfacePrimaryAlt = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#161922")
            : UIColor(hex: "#ffffff")
    }
    static let colorBorderPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#454f5e")
            : UIColor(hex: "#c4cedc")
    }
    static let colorBorderSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#22282f")
            : UIColor(hex: "#e0e6ef")
    }
    static let colorBorderBrandPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#7271e3")
            : UIColor(hex: "#655bd7")
    }
    static let colorBorderErrorPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#e94a55")
            : UIColor(hex: "#d5293d")
    }
    static let colorBorderWarningPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#f98824")
            : UIColor(hex: "#ec7004")
    }
    static let colorBorderSuccessPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#1cad79")
            : UIColor(hex: "#109367")
    }
    static let colorBorderBrandPrimaryAlt = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#454f5e")
            : UIColor(hex: "#655bd7")
    }
    static let colorBgHover = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff14")
            : UIColor(hex: "#0a0d140a")
    }
    static let colorBrandHover = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#655bd71f")
            : UIColor(hex: "#655bd714")
    }
    static let colorErrorHover = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#d5293d1f")
            : UIColor(hex: "#d5293d14")
    }
    static let colorWarningHover = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ec70041f")
            : UIColor(hex: "#ec700414")
    }
    static let colorSuccessHover = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#1093671f")
            : UIColor(hex: "#10936714")
    }
    static let colorBrandFocus = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#655bd729")
            : UIColor(hex: "#655bd729")
    }
    static let colorErrorFocus = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#d5293d29")
            : UIColor(hex: "#d5293d29")
    }
    static let colorWarningFocus = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ec700429")
            : UIColor(hex: "#ec700429")
    }
    static let colorSuccessFocus = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#10936729")
            : UIColor(hex: "#10936729")
    }
    static let colorSecondaryHover = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#0a0d1414")
            : UIColor(hex: "#0a0d1400")
    }
    static let colorFgFocus = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff1f")
            : UIColor(hex: "#0a0d141f")
    }
    static let colorBgActive = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#22282f")
            : UIColor(hex: "#f1f5f9")
    }
    static let colorFgHover = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff14")
            : UIColor(hex: "#0a0d140a")
    }
    static let colorFgInverseHover = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#0a0d140a")
            : UIColor(hex: "#ffffff14")
    }
    static let colorBgDisabled = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#0a0d1414")
            : UIColor(hex: "#0a0d140a")
    }
    static let colorFgDisabled = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff14")
            : UIColor(hex: "#0a0d140a")
    }
    static let colorBorderDisabled = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff1f")
            : UIColor(hex: "#0a0d141f")
    }
    static let colorContentDisabled = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff3d")
            : UIColor(hex: "#0a0d143d")
    }
    static let colorRosePrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#e6607f")
            : UIColor(hex: "#e24468")
    }
    static let colorRoseSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#77102e")
            : UIColor(hex: "#fbe4eb")
    }
    static let colorContentRose = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#941636")
    }
    static let colorPinkPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ff79d0")
            : UIColor(hex: "#f733b2")
    }
    static let colorPinkSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#6e0048")
            : UIColor(hex: "#ffe7f7")
    }
    static let colorContentPink = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#9b0065")
    }
    static let colorIndigoPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ff79d0")
            : UIColor(hex: "#bb3ded")
    }
    static let colorIndigoSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#550674")
            : UIColor(hex: "#f7e8fe")
    }
    static let colorContentIndigo = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#71089b")
    }
    static let colorPurplePrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#9e83c0")
            : UIColor(hex: "#7d5ba7")
    }
    static let colorPurpleSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#402759")
            : UIColor(hex: "#eee7f4")
    }
    static let colorContentPurple = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#50376f")
    }
    static let colorBluegrayPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#9fadcb")
            : UIColor(hex: "#7083ae")
    }
    static let colorBluegraySecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#2f3b55")
            : UIColor(hex: "#ebedf3")
    }
    static let colorContentBluegray = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#3d4c6e")
    }
    static let colorBluePrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#79c0ff")
            : UIColor(hex: "#339cf7")
    }
    static let colorBlueSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#003a6e")
            : UIColor(hex: "#e7f4ff")
    }
    static let colorContentBlue = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#00539b")
    }
    static let colorAquaPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#35f5ff")
            : UIColor(hex: "#00dde9")
    }
    static let colorAquaSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#00575c")
            : UIColor(hex: "#dafdff")
    }
    static let colorContentAqua = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#00868d")
    }
    static let colorGreenPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#7bc6bc")
            : UIColor(hex: "#50afa4")
    }
    static let colorGreenSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#205a5c")
            : UIColor(hex: "#e6f4f4")
    }
    static let colorContentGreen = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#2c746e")
    }
    static let colorCopperPrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#dea977")
            : UIColor(hex: "#ce8c4f")
    }
    static let colorCopperSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#633c18")
            : UIColor(hex: "#faf1e9")
    }
    static let colorContentCopper = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#835020")
    }
    static let colorOrangePrimary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#f7906a")
            : UIColor(hex: "#ec6330")
    }
    static let colorOrangeSecondary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#742306")
            : UIColor(hex: "#feece5")
    }
    static let colorContentOrange = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#ffffff")
            : UIColor(hex: "#9b2f08")
    }
    static let colorOrangeTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#c54516")
            : UIColor(hex: "#fddbcf")
    }
    static let colorCopperTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#a96c33")
            : UIColor(hex: "#f5e5d6")
    }
    static let colorGreenTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#388f85")
            : UIColor(hex: "#d6edeb")
    }
    static let colorAquaTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#00b5be")
            : UIColor(hex: "#bdfcff")
    }
    static let colorBlueTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#006dcc")
            : UIColor(hex: "#d1e9ff")
    }
    static let colorBlugaryTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#53658d")
            : UIColor(hex: "#dde1ec")
    }
    static let colorPurpleTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#634289")
            : UIColor(hex: "#e2d9ec")
    }
    static let colorIndigoTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#8e0ac2")
            : UIColor(hex: "#f1d3fd")
    }
    static let colorPinkTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#cc0085")
            : UIColor(hex: "#ffd1ef")
    }
    static let colorRoseTertiary = UIColor { tc in
        tc.userInterfaceStyle == .dark
            ? UIColor(hex: "#b7274a")
            : UIColor(hex: "#f8ced9")
    }
    static let spacing0: CGFloat = 0
    static let spacing2: CGFloat = 2
    static let spacing4: CGFloat = 4
    static let spacing8: CGFloat = 8
    static let spacing12: CGFloat = 12
    static let spacing16: CGFloat = 16
    static let spacing20: CGFloat = 20
    static let spacing24: CGFloat = 24
    static let spacing32: CGFloat = 32
    static let spacing36: CGFloat = 36
    static let spacing40: CGFloat = 40
    static let spacing48: CGFloat = 48
    static let spacing64: CGFloat = 64
    static let spacing80: CGFloat = 80
    static let spacing96: CGFloat = 96
    static let spacing112: CGFloat = 112
    static let spacing128: CGFloat = 128
    static let spacing160: CGFloat = 160
    static let typographyDisplayXlargeFontFamily: String = "inter"
    static let typographyDisplayXlargeFontSize: CGFloat = 96
    static let typographyDisplayXlargeFontWeight: CGFloat = 700
    static let typographyDisplayXlargeLineHeight: CGFloat = 112
    static let typographyDisplayXlargeLetterSpacing: CGFloat = -0.75
    static let typographyDisplayLargeFontFamily: String = "inter"
    static let typographyDisplayLargeFontSize: CGFloat = 64
    static let typographyDisplayLargeFontWeight: CGFloat = 700
    static let typographyDisplayLargeLineHeight: CGFloat = 72
    static let typographyDisplayLargeLetterSpacing: CGFloat = -0.75
    static let typographyDisplayMediumFontFamily: String = "inter"
    static let typographyDisplayMediumFontSize: CGFloat = 52
    static let typographyDisplayMediumFontWeight: CGFloat = 700
    static let typographyDisplayMediumLineHeight: CGFloat = 60
    static let typographyDisplayMediumLetterSpacing: CGFloat = -0.75
    static let typographyDisplaySmallFontFamily: String = "inter"
    static let typographyDisplaySmallFontSize: CGFloat = 44
    static let typographyDisplaySmallFontWeight: CGFloat = 700
    static let typographyDisplaySmallLineHeight: CGFloat = 52
    static let typographyDisplaySmallLetterSpacing: CGFloat = -0.75
    static let typographyHeadingXxlargeFontFamily: String = "inter"
    static let typographyHeadingXxlargeFontSize: CGFloat = 40
    static let typographyHeadingXxlargeFontWeight: CGFloat = 700
    static let typographyHeadingXxlargeLineHeight: CGFloat = 48
    static let typographyHeadingXxlargeLetterSpacing: CGFloat = -0.75
    static let typographyHeadingXlargeFontFamily: String = "inter"
    static let typographyHeadingXlargeFontSize: CGFloat = 36
    static let typographyHeadingXlargeFontWeight: CGFloat = 700
    static let typographyHeadingXlargeLineHeight: CGFloat = 44
    static let typographyHeadingXlargeLetterSpacing: CGFloat = -0.75
    static let typographyHeadingLargeFontFamily: String = "inter"
    static let typographyHeadingLargeFontSize: CGFloat = 32
    static let typographyHeadingLargeFontWeight: CGFloat = 700
    static let typographyHeadingLargeLineHeight: CGFloat = 40
    static let typographyHeadingLargeLetterSpacing: CGFloat = -0.75
    static let typographyHeadingMediumFontFamily: String = "inter"
    static let typographyHeadingMediumFontSize: CGFloat = 28
    static let typographyHeadingMediumFontWeight: CGFloat = 700
    static let typographyHeadingMediumLineHeight: CGFloat = 36
    static let typographyHeadingMediumLetterSpacing: CGFloat = -0.75
    static let typographyHeadingSmallFontFamily: String = "inter"
    static let typographyHeadingSmallFontSize: CGFloat = 24
    static let typographyHeadingSmallFontWeight: CGFloat = 700
    static let typographyHeadingSmallLineHeight: CGFloat = 32
    static let typographyHeadingSmallLetterSpacing: CGFloat = -0.75
    static let typographyHeadingXsmallFontFamily: String = "inter"
    static let typographyHeadingXsmallFontSize: CGFloat = 20
    static let typographyHeadingXsmallFontWeight: CGFloat = 700
    static let typographyHeadingXsmallLineHeight: CGFloat = 28
    static let typographyHeadingXsmallLetterSpacing: CGFloat = -0.75
    static let typographyLabelLargeFontSize: CGFloat = 18
    static let typographyLabelLargeFontFamily: String = "inter"
    static let typographyLabelLargeFontWeight: CGFloat = 500
    static let typographyLabelLargeLineHeight: CGFloat = 24
    static let typographyLabelLargeLetterSpacing: CGFloat = -0.05
    static let typographyLabelMediumFontFamily: String = "inter"
    static let typographyLabelMediumFontSize: CGFloat = 16
    static let typographyLabelMediumFontWeight: CGFloat = 500
    static let typographyLabelMediumLineHeight: CGFloat = 24
    static let typographyLabelMediumLetterSpacing: CGFloat = -0.05
    static let typographyLabelSmallFontFamily: String = "inter"
    static let typographyLabelSmallFontSize: CGFloat = 14
    static let typographyLabelSmallFontWeight: CGFloat = 500
    static let typographyLabelSmallLineHeight: CGFloat = 20
    static let typographyLabelSmallLetterSpacing: CGFloat = -0.05
    static let typographyLabelXsmallFontFamily: String = "inter"
    static let typographyLabelXsmallFontSize: CGFloat = 12
    static let typographyLabelXsmallFontWeight: CGFloat = 500
    static let typographyLabelXsmallLineHeight: CGFloat = 16
    static let typographyLabelXsmallLetterSpacing: CGFloat = -0.05
    static let typographyLabelMicroFontFamily: String = "inter"
    static let typographyLabelMicroFontSize: CGFloat = 10
    static let typographyLabelMicroFontWeight: CGFloat = 500
    static let typographyLabelMicroLineHeight: CGFloat = 16
    static let typographyLabelMicroLetterSpacing: CGFloat = -0.05
    static let typographyBodyLargeFontFamily: String = "inter"
    static let typographyBodyLargeFontSize: CGFloat = 18
    static let typographyBodyLargeFontWeight: CGFloat = 400
    static let typographyBodyLargeLineHeight: CGFloat = 28
    static let typographyBodyLargeLetterSpacing: CGFloat = 0
    static let typographyBodyMediumFontFamily: String = "inter"
    static let typographyBodyMediumFontSize: CGFloat = 16
    static let typographyBodyMediumFontWeight: CGFloat = 400
    static let typographyBodyMediumLineHeight: CGFloat = 24
    static let typographyBodyMediumLetterSpacing: CGFloat = 0
    static let typographyBodySmallFontFamily: String = "inter"
    static let typographyBodySmallFontSize: CGFloat = 14
    static let typographyBodySmallFontWeight: CGFloat = 400
    static let typographyBodySmallLineHeight: CGFloat = 20
    static let typographyBodySmallLetterSpacing: CGFloat = 0
    static let typographyBodyXsmallFontFamily: String = "inter"
    static let typographyBodyXsmallFontSize: CGFloat = 12
    static let typographyBodyXsmallFontWeight: CGFloat = 400
    static let typographyBodyXsmallLineHeight: CGFloat = 20
    static let typographyBodyXsmallLetterSpacing: CGFloat = 0
    static let borderWidth0: CGFloat = 0
    static let borderWidth05: CGFloat = 0.5
    static let borderWidth1: CGFloat = 1
    static let borderWidth2: CGFloat = 2
    static let borderWidth4: CGFloat = 4
    static let cornerRadius0: CGFloat = 0
    static let cornerRadius4: CGFloat = 4
    static let cornerRadius8: CGFloat = 8
    static let cornerRadius12: CGFloat = 12
    static let cornerRadius16: CGFloat = 16
    static let cornerRadius32: CGFloat = 32
    static let cornerRadius9999: CGFloat = 9999
    static let width0: CGFloat = 0
    static let width2: CGFloat = 2
    static let width4: CGFloat = 4
    static let width8: CGFloat = 8
    static let width12: CGFloat = 12
    static let width16: CGFloat = 16
    static let width20: CGFloat = 20
    static let width24: CGFloat = 24
    static let width32: CGFloat = 32
    static let width36: CGFloat = 36
    static let width40: CGFloat = 40
    static let width48: CGFloat = 48
    static let width64: CGFloat = 64
    static let width80: CGFloat = 80
    static let width96: CGFloat = 96
    static let width112: CGFloat = 112
    static let width128: CGFloat = 128
    static let width160: CGFloat = 160
    static let height0: CGFloat = 0
    static let height2: CGFloat = 2
    static let height4: CGFloat = 4
    static let height8: CGFloat = 8
    static let height12: CGFloat = 12
    static let height16: CGFloat = 16
    static let height20: CGFloat = 20
    static let height24: CGFloat = 24
    static let height32: CGFloat = 32
    static let height36: CGFloat = 36
    static let height40: CGFloat = 40
    static let height48: CGFloat = 48
    static let height64: CGFloat = 64
    static let height80: CGFloat = 80
    static let height96: CGFloat = 96
    static let height112: CGFloat = 112
    static let height128: CGFloat = 128
    static let height160: CGFloat = 160
}
